#!/bin/bash

echo "Adding this folder to the PATH environment variable..."
sleep 1

DOTFILES_DIR=${PWD}

echo "

" >> ~/.profile
echo "export DOTFILES_DIR=$DOTFILES_DIR" >> ~/.profile
echo "export PATH=$PATH:$DOTFILES_DIR:$DOTFILES_DIR/Scripts/bash-scripts:$DOTFILES_DIR/Scripts/setup-scripts:$DOTFILES_DIR/Scripts/setup-scripts" >> ~/.profile

source ~/.bashrc

# This is also done right now so that the rest of the script can go along nicely, even without a reboot
export DOTFILES_DIR=$DOTFILES_DIR
export PATH=$PATH:$DOTFILES_DIR:$DOTFILES_DIR/Scripts/bash-scripts:$DOTFILES_DIR/Scripts/setup-scripts

echo "Added!"
sleep 1
